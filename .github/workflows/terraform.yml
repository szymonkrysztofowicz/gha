name: 'Terraform GHA'
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name Checkout
        uses: actions/checkout@v3
  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: run a command
        runs: echo "Hello World!"
