name: 'Terraform GHA MAIN'
on:
  pull_request:
    branches:
      - 'main'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
  
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: Plan
        run: echo "Plan!"
  
  apply:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Apply
        run: echo "Auto Apply"
